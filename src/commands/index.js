const ping = require('./ping');
const playerLookup = require('./playerLookup');
const playerList = require('./playerList');
const about = require('./about');

module.exports = [ping, playerLookup, playerList, about];
